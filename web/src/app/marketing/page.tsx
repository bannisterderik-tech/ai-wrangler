export default function MarketingPage() {
  return (
    <div className="overflow-y-auto px-[18px] py-4">
      <div className="mb-3 text-[11.5px]" style={{ color: "var(--text-secondary)" }}>
        The AI drafts in each customer’s voice (it reads their brand files first). Nothing publishes without a human OK.
      </div>
      <div className="max-w-[640px] rounded-[10px] px-3.5 py-3 text-[12.5px]" style={{ background: "var(--surface-raised)", border: "1px solid var(--hairline)" }}>
        Calendar, watchtower, and uptime land here next. Isolation rule still applies: a Brightline draft never uses Harbor’s brand files.
      </div>
    </div>
  );
}
