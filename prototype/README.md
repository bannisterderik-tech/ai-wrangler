# Prototype (do not deploy)

Claude Design export of the NeXT Agency OS (`NeXT-Agency Demo.dc.html` + `support.js`).

Local preview of the sim:

```bash
python3 -m http.server 8899
```

Or `npm run prototype` from the repo root (legacy Node vault on 8899).

The production app is `../web`.
